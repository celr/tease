///<reference path="../AnimationRenderer.ts" />

// Generates the multi-browser HMTL code of a given enviroment.
class HTMLGenerator {

    public generateHMTL(enviroment: RenderedEnvironment) {
        var out = "";
        // TODO(chadan): Generate code.
        return out;
    }
}